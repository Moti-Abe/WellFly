import 'package:flutter/material.dart';

class MultiCitySearchPage extends StatelessWidget {
  const MultiCitySearchPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: Text("multi city")));
  }
}
